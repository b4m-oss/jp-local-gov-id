# Google Analytics 4 measurement ID (optional)
# Leave empty to disable analytics. For Netlify SSG, set this in Site settings → Environment variables
# so it is available at build time.
# Local `nuxt dev` never loads GA even if this is set (see plugins/google-analytics.client.ts).
NUXT_PUBLIC_SCRIPTS_GOOGLE_ANALYTICS_ID=
